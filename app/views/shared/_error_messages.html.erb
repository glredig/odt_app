<% if @user.errors.any? %>
	<div class="error_explanation">
		<div class="alert alert_error">
			There were errors in submitting your form
		</div>
		<ul>
			<% @user.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>